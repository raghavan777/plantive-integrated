// This can be extended later for AI crop analysis or metadata extraction
export const analyzeImage = async (imagePath) => {
    // Mock AI logic — replace with TensorFlow or OpenAI Vision if needed
    return {
        healthScore: Math.floor(Math.random() * 100),
        detectedIssues: ["none"],
    };
};
